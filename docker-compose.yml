services:
  opendonita:
    build: .
    container_name: opendonita
    ports:
      - "80:80"
      - "2008:2008"
    volumes:
      - ./opendonita-data:/root/.opendonita
    restart: unless-stopped
